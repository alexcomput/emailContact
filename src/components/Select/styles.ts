import styled from 'styled-components';
import { shade } from 'polished';
import { LinkProps, Link } from 'react-router-dom';
import { LinkHTMLAttributes } from 'react';

export const Container = styled.div`
  .select-target__control {
    border: 0;
  }
`;
